<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href={{absURL "/assets/css/style.css"}}>
	<title>Portfolio</title>
</head>
<body>
    <header>
        <h1>Maxime Pigeon</h1>
        <ol>
            {{range .Site.Projects}}
                <li>
                    <a href="{{absURL .Slug}}">{{.Title}}</a>
                </li>
            {{end}}
        </ol>
    </header>
</body>
</html>